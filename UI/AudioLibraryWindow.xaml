<Window x:Class="UI.AudioLibraryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="AudioLibraryWindow" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Content="Добавить папку" Click="ImportFolder_Click" />
            <Button Content="Редактировать трек" Click="EditTags_Click" Margin="6,0,0,0" />
            <Button Content="Удалить" Click="Delete_Click" Margin="6,0,0,0" />
        </StackPanel>

        <DataGrid x:Name="AudioFilesDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Исполнитель" Binding="{Binding Author}" />
                <DataGridTextColumn Header="Альбом" Binding="{Binding Album.Name}" />
                <DataGridTextColumn Header="URL" Binding="{Binding URL}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Музыкальный плеер" Height="620" Width="950"
        Background="{StaticResource BackgroundDarkBrush}">
    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBox x:Name="SearchTextBox" Width="300" Height="32"
                     Text="Поиск..." />
            <Button Content="Поиск" Click="SearchButton_Click"/>
            <Button Content="Создать плейлист" Click="CreatePlaylist_Click"/>
            <Button Content="Библиотека" Click="OpenLibrary_Click"/>
            <Button Content="Премиум" Click="OpenPayment_Click"/>
            <Button Content="Плейлисты" Click="OpenPlaylistManager_Click"/>
        </StackPanel>

        <DataGrid x:Name="MainDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  Margin="0,10,0,10"
                  MouseDoubleClick="MainDataGrid_MouseDoubleClick"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  EnableRowVirtualization="True">

            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3A3A3A"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>

                <Style TargetType="DataGridCell">
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTemplateColumn Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Width="50" Height="50"
                       Source="{Binding CoverImage,
                       Converter={StaticResource ByteArrayToImageConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


                <DataGridTextColumn Header="Название"
                        Binding="{Binding Name}"
                        Width="2*"/>


                <DataGridTextColumn Header="Исполнитель"
                        Binding="{Binding Author}"
                        Width="*"/>

            </DataGrid.Columns>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Foreground" Value="#D6D6D6"/>
                    <Setter Property="Background" Value="#2A2A2A"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="FontSize" Value="14"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

        </DataGrid>



        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="50 5 50 10">
            <Button Content="⏮" Width="40" Click="PreviousButton_Click"/>
            <Button Content="⏯" Width="40" Click="PlayButton_Click"/>
            <Button Content="⏭" Width="40" Click="NextButton_Click"/>

            <TextBlock x:Name="NowPlayingText" Margin="20,0" VerticalAlignment="Center"
                       Foreground="White" Width="400"/>

            <Slider x:Name="VolumeSlider"
                    Minimum="0" Maximum="1" Value="0.5" Width="150"
                    ValueChanged="VolumeSlider_ValueChanged"
                    Margin="20,20"/>

            

            


        </StackPanel>
        <MediaElement x:Name="Player"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Stop"
                      Visibility="Collapsed"
                      MediaOpened="Player_MediaOpened"/>


        <StackPanel Grid.Row="3" Margin="50 5 50 10">

            <Slider x:Name="TimelineSlider"
                    Minimum="0"
                    ValueChanged="TimelineSlider_ValueChanged"
                    PreviewMouseDown="Timeline_PreviewMouseDown"
                    PreviewMouseUp="Timeline_PreviewMouseUp"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="CurrentTimeText" Text="00:00"/>
                <TextBlock Text=" / "/>
                <TextBlock x:Name="TotalTimeText" Text="00:00"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>

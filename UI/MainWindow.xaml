<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Музыкальный плеер" Height="600" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Верхняя панель поиска и кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="SearchTextBox" Width="300" Height="28" VerticalAlignment="Center" Text="Поиск по треку/исполнителю" />
            <Button Content="Поиск" Margin="6,0,0,0" Click="SearchButton_Click" />
            <Button Content="Создать плейлист" Margin="10,0,0,0" Click="CreatePlaylist_Click" />
            <Button Content="Управление библиотекой" Margin="10,0,0,0" Click="OpenLibrary_Click" />
            <Button Content="Премиум" Margin="10,0,0,0" Click="OpenPayment_Click" />
        </StackPanel>

        <!-- Таблица треков -->
        <DataGrid x:Name="MainDataGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  MouseDoubleClick="MainDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Исполнитель" Binding="{Binding Author}" Width="200" />
                <DataGridTextColumn Header="Альбом" Binding="{Binding Album.Name}" Width="150" />
                <DataGridTextColumn Header="URL" Binding="{Binding URL}" Width="200" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Нижняя панель воспроизведения -->
        <StackPanel Grid.Row="2" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,6,0,0">
                <Button Content="Previous" Click="PreviousButton_Click" />
                <Button Content="Play" Margin="6,0,0,0" Click="PlayButton_Click" />
                <Button Content="Pause" Margin="6,0,0,0" Click="PauseButton_Click" />
                <Button Content="Next" Margin="6,0,0,0" Click="NextButton_Click" />

                <TextBlock x:Name="NowPlayingText" Margin="12,0,0,0" VerticalAlignment="Center" />

                <Slider x:Name="VolumeSlider" Minimum="0" Maximum="1" Value="0.5" Width="150" Margin="20,0,0,0" ValueChanged="VolumeSlider_ValueChanged" />
            </StackPanel>

            <!-- MediaElement (скрытый) для воспроизведения -->
            <MediaElement x:Name="Player" LoadedBehavior="Manual" UnloadedBehavior="Stop" Visibility="Collapsed" />
        </StackPanel>
    </Grid>
</Window>
